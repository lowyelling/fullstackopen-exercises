# 0.5 – Spa Diagram

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note over browser,server: User navigates to https://studies.cs.helsinki.fi/exampleapp/spa

    browser->>server: GET /spa
    activate server
    server-->>browser: HTML document
    deactivate server

    browser->>server: GET /main.css
    activate server
    server-->>browser: CSS file
    deactivate server

    Note right of browser: Browser parses HTML and finds <script src="spa.js">

    browser->>server: GET /spa.js
    activate server
    server-->>browser: JavaScript file (SPA logic)
    deactivate server

    Note right of browser: Browser starts executing spa.js

    browser->>server: GET /data.json
    activate server
    server-->>browser: JSON (notes data)
    deactivate server

    Note right of browser: spa.js builds the notes UI in the DOM (no full page reloads)
